Express generator:
    (do all steps in cmd)
    1. npm i express-generator or npm i express-generator -g (for global)
    2. create new app:
        express appname --view=ejs
    3. run two commands:
        i. cd appname
        ii. npm i